Tarefa:
1) Na rota/página "/clients", identifique o botão com rótulo/label "Todos" e detalhe integralmente o estilo e as cores atualmente aplicadas.
2) Liste literalmente todos os botões no projeto que tenham exatamente o mesmo estilo do botão "Todos".
3) Para esses botões (e somente eles), remova completamente as referências de cores/estilo atuais ligadas a sistema de cores (tokens, variáveis, tema, classes utilitárias de cor, mixins) e passe a usar diretamente no código as cores abaixo:
   - Background: #EBEBEB
   - Textos e ícones (incluindo SVG fill/stroke): #060606

Escopo e critérios:
- “Mesmo estilo” significa mesma aparência efetiva: mesmas propriedades visuais de cor (background, texto/ícone, borda se houver), tipografia, padding, raio de borda, sombra e estados (normal, hover, active, focus, disabled). Se forem instâncias do mesmo componente/variante, conte como iguais.
- Alterar somente os botões que cumprirem o critério acima. Não modifique nenhum outro componente.
- Mantenha todos os estilos que não sejam de cor (layout, tipografia, espaçamentos, raios, sombras) como estão. O que muda é exclusivamente a aplicação de cor.
- Remova qualquer hierarquia/referência de cor (tokens, CSS vars, tema, mixins) nesses botões e substitua por valores hex diretamente no ponto de aplicação (inline style, regra CSS, CSS-in-JS, utility específica com hex).
- Aplique as cores fornecidas em todos os estados do botão (normal, hover, active, focus, disabled) quando houver variação de cor; se o estado já usa a mesma cor, mantenha.
- Ícones: garantir fill e/ou stroke #060606.

Entregáveis (em ordem):
1) Relatório do estilo do botão "Todos":
   - Onde está definido (paths de arquivos, seletor/componente/variante).
   - Propriedades relevantes: background, text/icon color, border (se houver), tipografia, padding, radius, shadow, e cores/estilos por estado (hover/active/focus/disabled).
2) Lista EXAUSTIVA de botões com o mesmo estilo:
   - Para cada um: label/aria-label, rota/página, caminho do arquivo, seletor/componente/variante, e a justificativa de equivalência (ex.: “mesmo componente/variante” ou “mesmas propriedades visuais”).
3) Patches/diffs das alterações de cor:
   - Diffs por arquivo (unified diff), mostrando claramente a remoção de referências a tokens/variáveis/tema e a inclusão dos valores hex (#EBEBEB e #060606) no próprio código do botão/estilo.
   - Listar todos os arquivos alterados.
4) Verificação:
   - Confirmação de que nenhuma outra parte do sistema de cores foi alterada.
   - Confirmação de que somente os botões listados foram modificados.
   - Snapshot resumido de antes/depois das cores por estado.

Restrições:
- Não introduzir novas variáveis, tokens, mapas, mixins, helpers ou classes globais de cor.
- Não alterar o design de outros componentes/botões.
- Não mover lógica/estrutura; apenas substituir a aplicação de cores nos botões elegíveis.
- Não propor alternativas de paleta, acessibilidade ou design — usar exatamente:
  - Background: #EBEBEB
  - Textos e ícones: #060606

Se algo estiver ausente:
- Se o botão "Todos" não for encontrado, informar “Botão ‘Todos’ não encontrado em /clients” e listar possíveis equivalentes por label/aria-label próximos, com localização.
- Se não houver botões com o mesmo estilo, afirmar explicitamente e não alterar nada.