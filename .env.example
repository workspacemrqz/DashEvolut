# Database Configuration (OBRIGATÓRIO)
DATABASE_URL=postgresql://username:password@host:port/database

# Session Configuration (OBRIGATÓRIO para produção)
SESSION_SECRET=your-secure-random-secret-key-here

# Authentication (OBRIGATÓRIO)
LOGIN=admin
SENHA=your-secure-password-here

# Webhook Configuration (Opcional)
WebhookProposta=https://your-webhook-url.com/webhook/endpoint

# Landing Page Database (Opcional - somente se usar funcionalidade de landing pages)
DatabaseLandingPage=postgresql://username:password@host:port/database

# Server Configuration (definido automaticamente pelo Easypanel/Heroku)
# PORT=3000
NODE_ENV=production